# ── Core scientific stack ─────────────────────────────────────────────────────
numpy==2.5.3
pandas==3.0.6
xarray==2026.7.0
scipy>=1.13.0
netCDF4>=1.7.0
h5netcdf>=1.3.0
dask[array]>=2024.0.0
zarr>=2.18.0

# ── Regridding / GIS ──────────────────────────────────────────────────────────
# NOTE: xesmf is NOT used (requires ESMF compiled library, not pip-installable on Windows)
# pyresample is used instead — equivalent bilinear/nearest regridding, pip-installable
pyresample>=3.1.0
shapely>=2.0.0
cartopy>=0.23.0

# ── Deep learning ────────────────────────────────────────────────────────────
torch==2.4.1
# torch-geometric: install separately after torch (see README)
# pip install torch-geometric==2.6.1

# ── Visualization & logging ───────────────────────────────────────────────────
matplotlib>=3.9.0
tensorboard>=2.17.0
tqdm>=4.66.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0

# ── ML utilities ─────────────────────────────────────────────────────────────
scikit-learn>=1.5.0
